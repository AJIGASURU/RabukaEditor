using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//チェックポイントの親
public class ObjectInstructor : MonoBehaviour
{
    public GameObject targetObject;//指示先ターゲット

    //判定用に、なんのインストラクタなのか列挙したい

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
